N = int(input()) # NxN 체스판 위에 퀸 N개
N_mat = [[0] * N for _ in range(N)]

def queen():
    # visited = [[0, 0]]

    for y in range(N):
        for x in range(N):
            if visited[0][0] != N_mat[y][]
